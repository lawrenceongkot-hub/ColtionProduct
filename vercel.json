{
  "version": 2,
  "buildCommand": "npx prisma db push --schema=api/prisma/schema.prisma --accept-data-loss 2>/dev/null; npx prisma generate --schema=api/prisma/schema.prisma 2>/dev/null; vite build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.mjs"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.mjs": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}

{
  "version": 2,
  "buildCommand": "cd api && npx prisma generate --schema=prisma/schema.prisma && cd .. && npm run postinstall && vite build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api/index.mjs"
    },
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ],
  "functions": {
    "api/index.mjs": {
      "memory": 512,
      "maxDuration": 30
    }
  }
}